<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:paddingBottom="@dimen/activity_vertical_margin"
android:paddingLeft="@dimen/activity_horizontal_margin"
android:paddingRight="@dimen/activity_horizontal_margin"
android:paddingTop="@dimen/activity_vertical_margin"
android:background="@color/colorPrimaryDark"
android:orientation="vertical"
tools:context=".Benchmark"
    android:weightSum="1">

    <TextView
    android:id="@+id/txtTitle"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Benchmark_Sequence.Activate"
    android:textSize="25dp"
    android:textColor="@color/codecolour"
    android:textAppearance="@style/TextAppearance.AppCompat.Body2" />

    <Button
        android:text="Click here to Initiate processing power generation"
        android:textColor="@color/codecolour"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="onBeginClick"
        android:id="@+id/btnStart"
        android:background="@android:drawable/editbox_dropdown_light_frame"
        android:elevation="0dp"
        android:layout_weight="0.03"
        tools:background="@color/colorPrimary" />

    <TextView
    android:id="@+id/txtTerminal"
        android:textColor="@color/codecolour"
        android:layout_width="376dp"
        android:layout_marginTop="30dp"
        android:layout_height="392dp"
        android:hapticFeedbackEnabled="false"
        android:layout_weight="0.53"
        android:background="@drawable/terminalborder"/>

    <Button
        android:text="Proceed to machine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btnNext"
        android:onClick="sendMessage"
        tools:textColor="@color/codecolour"
        tools:background="@color/colorPrimary"/>

</LinearLayout>